{
  "name": "ai-prompt-gateway",
  "version": "1.0.0",
  "description": "Gateway that fans a prompt out to multiple AI providers, compares responses, and lets a user pick the best one. Built as a CI/CD demo project (Jenkins -> Docker -> Kubernetes -> Prometheus/Grafana).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test/smoke.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "prom-client": "^15.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}

